{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="form-group">
    {{ form_row(form.nbr_etoile, {'attr': {'class': 'form-control','id':"exampleInputUsername1"}}) }}
</div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
